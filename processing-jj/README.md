# script files for processing jiujitsu videos

`zscript-processing.sh`
1. compress all files first, mv origs to trash
2. use compressed file to generate no audio file
3. concatenate donation links to begining and end, suffix with ytready, move one dir lvl up

`zscript-compress.sh`
- just a compression

`zscript_rename.sh`
- don't need this technically, was shuttled into overall processing

`zscript_add_dash.sh`
- reformats notes from apple to be obsidian friendly